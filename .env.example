# --- Obrigatório ---------------------------------------------------------
# Quem recebe a mensagem diária. Use o nome exatamente como aparece na lista
# de conversas do TikTok. Vários destinatários: separe por vírgula.
TIKTOK_TARGETS=

# Mensagens possíveis, separadas por "|". Uma é sorteada a cada envio.
TIKTOK_MESSAGES=🔥

# --- Navegador -----------------------------------------------------------
# Canal do Chromium instalado na máquina: msedge, chrome ou vazio para usar
# o Chromium que vem com o Playwright (npm run setup).
BROWSER_CHANNEL=msedge

# Deixe false até confirmar que funciona; depois mude para true nas execuções
# agendadas, que ficam bem mais rápidas sem interface.
HEADLESS=false

# Pasta onde a sessão logada fica salva. Nunca versione.
BROWSER_PROFILE_DIR=.profile

# --- Tempos --------------------------------------------------------------
TIMEOUT_MS=45000

# Ritmo humano. O objetivo não é ser lento, é não ser instantâneo:
# ação instantânea é a assinatura mais óbvia de automação.

# Intervalo entre cada caractere digitado.
TYPING_MIN_MS=90
TYPING_MAX_MS=260

# Pausa ao carregar a caixa de mensagens e ao abrir uma conversa.
READ_MIN_MS=1800
READ_MAX_MS=4200

# Pausa antes de apertar Enter.
PRE_SEND_MIN_MS=800
PRE_SEND_MAX_MS=2000

# Pausa entre um destinatário e o próximo.
DELAY_MIN_MS=6000
DELAY_MAX_MS=18000

# Atraso aleatório no início, em ms (0 = desligado). Recomendado na execução
# agendada, para não disparar sempre no mesmo segundo. 900000 = até 15 min.
START_JITTER_MAX_MS=0

# --- Diagnóstico ---------------------------------------------------------
# true abre a conversa mas não envia nada: útil para testar a configuração.
DRY_RUN=false
SCREENSHOT_DIR=logs

# --- URLs (raramente precisa mexer) --------------------------------------
TIKTOK_MESSAGES_URL=https://www.tiktok.com/messages?lang=pt-BR
TIKTOK_LOGIN_URL=https://www.tiktok.com/login?lang=pt-BR
